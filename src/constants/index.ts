import { cloudinaryConstants } from './cloudinary.constants';
import { commonConstants } from './common.constants';
import { emailConstants } from './email.constants';
import { errorsConstants } from './errors.constants';
import { multerConstants } from './multer.constants';
import { orderConstants } from './order.constants';
import { pictureConstants } from './picture.constants';
import { productConstants } from './product.constants';
import { regexpConstants } from './regexp.constant';
import { reviewConstants } from './review.constants';
import { telegramConstants } from './telegram.constants';
import { userConstants } from './user.constants';

export const CONST = {
  Regexp: regexpConstants,
  Common: commonConstants,
  Errors: errorsConstants,
  Multer: multerConstants,
  Email: emailConstants,
  Cloudinary: cloudinaryConstants,
  Telegram: telegramConstants,
  User: userConstants,
  Product: productConstants,
  Review: reviewConstants,
  Order: orderConstants,
  Picture: pictureConstants,
};
